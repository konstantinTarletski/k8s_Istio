
Need to hack if run locally
Not working on Windows
-----------------------------------------------------------
kubectl edit svc istio-ingressgateway -n istio-system

```
 - name: http2
    nodePort: 32066
    port: 8085      # 80 --> 8085
    protocol: TCP
    targetPort: 8080
```
8085 --- > istio-gateway.yaml set here same
-----------------------------------------------------------

apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: skais-gateway
  namespace: skais-2-test
spec:
  # Demo Only !!
  selector:
    istio: ingressgateway
  servers:
    - port:
        number: 8085
        name: http
        protocol: HTTP
      hosts:
        - "*"
